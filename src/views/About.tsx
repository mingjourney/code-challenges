// 假设 About 是一个 React 组件
import React from 'react'

// 定义 About 组件类型
type AboutProps = {
  // 定义组件的 props（如果有）
}

// 创建 About 组件
const About: React.FC<AboutProps> = () => {
  // 组件内容
  return (
    // JSX
    <div>About</div>
  )
}

export default About
